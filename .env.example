# Option 1: place key directly in env (avoid committing real values)
MASSIVE_API_KEY=replace_with_real_key

# Option 2: point to key file path (preferred if you already use stoptions_analyzer)
MASSIVE_API_KEY_FILE=C:\Users\wbott\.stoptions_analyzer\api_key.txt
